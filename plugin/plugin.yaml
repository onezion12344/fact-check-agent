name: double-check
version: "2.0.0"
description: >
  Double-Check v2.0 — Unified Fact, Theory & Tool-Use verification.
  LLM classify → inject delegate instruction → sub-agent deep verification.
  No regex, no built-in search. Plugin is orchestrator, not worker.
requires_env: []
